import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.jaegervisionpro.app',
  appName: 'Jaeger Vision Pro',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
